<%= autocomplete(:manufacturer, f) %>
<%= autocomplete(:frame, f) %>
<%= autocomplete(:components, f) %>
<%= autocomplete(:color, f) %>
<%= label_text(:serial, f) %>
<i class="hint">* leave blank to autofill serial with Time.now</i><br>
<%= label_text(:year, f) %>
<%= label_text(:price, f) %>

<%= f.label :discipline_ids, "Choose disciplines:"%> <br>
<%= f.collection_check_boxes :discipline_ids, Discipline.all, :id, :name %> <br>
or enter new disciplines. <br>
Multiple disciplines can be added <br>
(ex: cyclocross gravel off-road would add 3 discplines) <br>
<%= label_tag "discpline[names]", "Disciplines"%>
<%= text_field_tag "discipline[names]" %> <br>

<!--
t.string "manufacturer"
t.string "frame"
t.integer "year"
t.integer "serial"
t.string "componenets"
t.integer "size"
t.integer "price"
t.string "color"
-->